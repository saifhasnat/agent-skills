# Global Claude Instructions

## Skills
Skills for this system are maintained in:
`/Users/saifhasnat/Documents/Projects/agent-skills/`

Installed globally: `agent-skill-builder`, `firebase-standard-manager`, `premium-ui-polisher`, `humanizer`.

## Workflow
- Explore and plan before coding for any multi-file change.
- Always verify work: run tests, compare screenshots, or check command output.
- Use `/clear` between unrelated tasks.
- Commit with descriptive messages. Open PRs after implementation.

## Code Style
- Prefer explicit over implicit.
- No hardcoded secrets — use environment variables.
- Write tests alongside new features when a test suite exists.
